---
#add dataset same of the .bed file as stored in /beds/ without the .bed extension
datasets: 
   gnomad

#see README for the download links of these references
reference-path:
  /dependencies
 
parameters:
   matrix: "yes" #set to "no" when matrix should be deleted once cadd-sv scoring is completed
 ...
